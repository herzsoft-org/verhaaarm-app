name: verhaaarm
description: "Verhåårm App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  jwt_decode: ^0.3.1

  cupertino_icons: ^1.0.8

  dio: ^5.7.0
  flutter_secure_storage: ^10.0.0
  go_router: ^17.0.1
  intl: ^0.20.2

  camera: ^0.11.0+2
  image_picker: ^1.1.2

  path_provider: ^2.1.4
  share_plus: ^12.0.1

  package_info_plus: ^9.0.0
  device_info_plus: ^12.3.0
  url_launcher: ^6.3.2
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
